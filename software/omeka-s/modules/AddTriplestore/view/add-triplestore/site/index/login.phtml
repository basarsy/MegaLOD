<?php

$this->headTitle('Login - ' . $site->title());
?>

<div class="auth-container">
    <div class="auth-card">
        <div class="auth-header">
            <h2>🔑 Login to Archaeological Data Management</h2>
            <p>Access your account to add excavations and artifacts</p>
        </div>

        <?php if (isset($error)): ?>
            <div class="alert alert-error">
                <p><?php echo $this->escapeHtml($error); ?></p>
            </div>
        <?php endif; ?>
        
        <?php if (isset($message)): ?>
            <div class="alert alert-success">
                <p><?php echo $this->escapeHtml($message); ?></p>
            </div>
        <?php endif; ?>

        <?php echo $this->form()->openTag($form->setAttribute('class', 'auth-form')); ?>

        <div class="form-group">
            <label for="email">Email Address</label>
            <input type="email" name="email" id="email" class="form-control" required 
                   placeholder="name@example.com" value="<?php echo $this->escapeHtml($form->get('email')->getValue()); ?>">
        </div>

        <div class="form-group">
            <label for="password">Password</label>
            <input type="password" name="password" id="password" class="form-control" required 
                   placeholder="••••••••">
        </div>

        <div class="form-actions">
            <?php echo $this->formHidden($form->get('loginform_csrf')); ?>
            <button type="submit" class="btn-primary full-width">Sign In</button>
        </div>

        <?php echo $this->form()->closeTag(); ?>

        <div class="auth-links">
            <p>Don't have an account? 
                <a href="<?php echo $this->url('site/add-triplestore/signup', ['site-slug' => $site->slug()]); ?>" 
                   class="auth-link">Sign up here</a>
            </p>
            <p>
                <a href="<?php echo $this->url('site', ['site-slug' => $site->slug()]); ?>" 
                   class="auth-link">← Back to Main Site</a>
            </p>
        </div>
    </div>
</div>