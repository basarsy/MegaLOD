<?php
$this->headTitle('Sign Up - ' . $site->title());
?>

<div class="auth-container">
    <div class="auth-card">
        <div class="auth-header">
            <h2>📝 Create Your Account</h2>
            <p>Join to contribute archaeological data</p>
        </div>
        
        <?php if (isset($error)): ?>
            <div class="alert alert-error">
                <p><?php echo $this->escapeHtml($error); ?></p>
            </div>
        <?php endif; ?>
        
        <?php if (isset($message)): ?>
            <div class="alert alert-success">
                <p><?php echo $this->escapeHtml($message); ?></p>
            </div>
        <?php endif; ?>

        <?php echo $this->form()->openTag($form->setAttribute('class', 'auth-form')); ?>
        <input type="hidden" name="csrf_token" value="<?php echo $this->escapeHtml($csrfToken); ?>">

        <div class="form-group">
            <label for="name">Full Name</label>
            <input type="text" name="name" id="name" class="form-control" required 
                   placeholder="John Doe" value="<?php echo $this->escapeHtml($form->get('name')->getValue()); ?>">
        </div>

        <div class="form-group">
            <label for="email">Email Address</label>
            <input type="email" name="email" id="email" class="form-control" required 
                   placeholder="name@example.com" value="<?php echo $this->escapeHtml($form->get('email')->getValue()); ?>">
        </div>

        <div class="form-group">
            <label for="password">Password</label>
            <input type="password" name="password" id="password" class="form-control" required 
                   placeholder="••••••••" minlength="8">
            <small class="help-text">Must be at least 8 characters long</small>
        </div>

        <div class="form-group">
            <label for="confirm_password">Confirm Password</label>
            <input type="password" name="confirm_password" id="confirm_password" class="form-control" required 
                   placeholder="••••••••">
        </div>

        <div class="form-actions">
            <button type="submit" class="btn-primary full-width">Create Account</button>
        </div>

        <?php echo $this->form()->closeTag(); ?>

        <div class="auth-links">
            <p>Already have an account? 
                <a href="<?php echo $this->url('site/add-triplestore/login', ['site-slug' => $site->slug()]); ?>" 
                   class="auth-link">Login here</a>
            </p>
            <p>
                <a href="<?php echo $this->url('site', ['site-slug' => $site->slug()]); ?>" 
                   class="auth-link">← Back to Main Site</a>
            </p>
        </div>
    </div>
</div>

<script>
document.getElementById('confirm_password').addEventListener('input', function() {
    const password = document.getElementById('password').value;
    const confirmPassword = this.value;
    
    if (password !== confirmPassword) {
        this.setCustomValidity('Passwords do not match');
    } else {
        this.setCustomValidity('');
    }
});
</script>